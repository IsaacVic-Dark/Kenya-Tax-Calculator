services:
  - type: web
    name: kenya-tax-calculator
    env: php
    plan: free
    buildCommand: ""  # No build step needed
    startCommand: "php -S 0.0.0.0:10000"  # Render handles PHP automatically
